import random
r=random.randint(0,6)
print(r)
	if(r==1):
		print("one run,good try")
	if(r==2):
		print("two runs....")
	if(r==3):
		print("its gone way from the fielder,three runs.. ")
	if(r==4):
		print("its four..,good drive")
	if(r==5):
		print("its gone way from keeper and runs away four,wide")
	if(r==6):
		print("its gone away for six,its huge")
